# 项目约定（mini-mlmath）

## 语言
- 始终用中文思考和回复用户。
- 代码、变量名、注释、commit message 等保留英文习惯，仅对话和解释用中文。

## 每次新增知识的必做清单（防止遗漏）

任何新增或修改——包括新头文件、新文档、新配图、新测试程序——完成后必须：

1. **同步更新 README.md**（三处都要检查）：
   - 「目录结构」树：新增的头文件 / 文档 / 测试程序都要补进对应分支
   - 「文档」章节：新文档加链接，含一句话说明
   - 「编译」章节：新增的测试可执行补进运行命令列表
2. **git add + git commit + git push**，commit message 用中文、按仓库现有风格
   （格式：`模块: 做了什么`，可带正文要点）。
3. 提交前用 `git status` 确认没有漏掉文件。

## 代码风格
- 沿用仓库约定：header-only、无宏（除 check.h 的 CHECK）、无表达式模板、
  eager 求值；教学注释尽量详尽。
- 新配图用手写 SVG（docs/images/，零依赖），风格与现有图一致。
